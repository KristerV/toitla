template(name="mainMenu")
	.menu
		if currentUser
			+menuItem(panel="left" content="userProfileEdit" icon=profileImage contentClass="{{#if profileImage}}cover{{/if}}")
			.align-bottom(style="width:{{menubarWidth}}")
				+menuItem(panel="left" content="postNew" icon="/icons/white-camera.svg")
		else
			+menuItem(panel="left" content="login" icon="/icons/white-toitla.svg")